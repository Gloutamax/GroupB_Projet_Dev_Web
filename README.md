# Nom et prénom des membres du groupe
- Gloutamax : Justin DAUB
- Motys8 : Hugo CHI 
- Yvan134 : Yvan BELL YEPP

# Installation des bilibohtèques pour le backend 
Nous avons utilisée docker pour pouvoir effectuer l'initialisation du serveur, avec la commande
``` bash
docker compose run --rm backend npm install express pg sequelize jsonwebtoken bcrypt
```
